name: Analyzer
on: [push, pull_request]

jobs:
  package-analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: axel-op/dart_package_analyzer@stable
        with:
          githubToken: ${{ secrets.Analyzer }}
          maxScoreToComment: 99.99
